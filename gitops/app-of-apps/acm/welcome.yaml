---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: welcome
  namespace: openshift-gitops
spec:
  generators:
  - clusters: {} 
  syncPolicy:
    preserveResourcesOnDeletion: false
  template:
    metadata:
      name: welcome
      annotations:
        argocd.argoproj.io/compare-options: IgnoreExtraneous
    spec:
      project: default
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: openshift-gitops
      source:
        repoURL: https://github.com/wcushen/acm-sno-lab.git
        path: gitops/applications/welcome/overlay/a-team
        plugin:
          name: sops-age-kustomize-plugin
        targetRevision: feature/sans-acm
      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - Validate=true